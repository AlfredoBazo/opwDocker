import { JSONObject, TinyType } from 'tiny-types';
/**
 * @access public
 */
export declare abstract class Tag extends TinyType {
    readonly name: string;
    readonly type: string;
    static fromJSON(o: JSONObject): any;
    protected constructor(name: string, type: string);
    toJSON(): {
        name: string;
        type: string;
    };
}
